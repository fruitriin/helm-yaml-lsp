{
  "name": "helm-yaml-lsp-server",
  "version": "0.18.0",
  "description": "Argo Workflows Language Server for Helm and Kubernetes YAML",
  "license": "MIT",
  "main": "dist/server.js",
  "bin": {
    "helm-yaml-lsp-server": "dist/server.js"
  },
  "files": [
    "dist/**"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fruitriin/helm-yaml-lsp.git",
    "directory": "packages/server"
  },
  "homepage": "https://github.com/fruitriin/helm-yaml-lsp#readme",
  "bugs": {
    "url": "https://github.com/fruitriin/helm-yaml-lsp/issues"
  },
  "keywords": [
    "argo",
    "workflows",
    "kubernetes",
    "helm",
    "yaml",
    "lsp",
    "language-server"
  ],
  "scripts": {
    "build": "bun build src/server.ts --outdir dist --target node --format cjs --sourcemap=external && printf '#!/usr/bin/env node\\n' | cat - dist/server.js > dist/server.tmp && mv dist/server.tmp dist/server.js",
    "watch": "bun build src/server.ts --outdir dist --target node --format cjs --sourcemap=external --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "biome lint src",
    "format": "biome format src"
  },
  "dependencies": {
    "fast-glob": "^3.3.3",
    "js-yaml": "^4.1.0",
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-protocol": "^3.17.5",
    "vscode-languageserver-textdocument": "^1.0.11",
    "vscode-languageserver-types": "^3.17.5"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9"
  }
}
